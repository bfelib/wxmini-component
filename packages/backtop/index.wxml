<view>
  <scroll-view
    scroll-y="true"
    style="height: {{ height }}"
    scroll-top="{{ scrollTop }}"
    bindscroll="scroll"
    scroll-with-animation="true"
  >
    <slot></slot>
  </scroll-view>

  <view
    class="backtop {{ isBack ? 'show' : '' }}"
    style="right: {{ right }}; bottom: {{ bottom }}; z-index: {{ zIndex }};"
    bindtap="click"
  >
    <text>⇧</text>
  </view>
</view>
